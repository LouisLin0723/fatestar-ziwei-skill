# FateStar API Key Configuration
# =================================
# 排盘 (chart / transits) 匿名免费, 无需 key。
# 仅「郑大钱」解读 (reading) 需要 fs_live_ key, 按积分计费。
#
# 申请 key: 到 https://www.fatestar.top 注册免费会员 → 做新手任务领积分
#           → 开发者中心创建固定的 fs_live_ key (可重复使用)。
#
# Priority: --api_key flag > .env file > system environment variable > anonymous
#
# Format:
# FATESTAR_API_KEY=fs_live_xxxxxxxx

FATESTAR_API_KEY=
